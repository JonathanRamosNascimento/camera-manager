# Build
/target/

# Configuração com credenciais reais do NVR — NUNCA versionar.
/config/cameras.toml
/config/*.local.toml

# Capturas geradas em runtime
/snapshots/

# Pacotes gerados (deb, dmg, zip, instaladores)
/dist/
/packaging/arch/src/
/packaging/arch/pkg/
/packaging/arch/*.pkg.tar.*
